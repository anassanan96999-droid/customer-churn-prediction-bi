# Copy to .streamlit/secrets.toml (git-ignored) or paste into
# Streamlit Cloud -> App settings -> Secrets. Optional: without it the
# dashboard writes retention briefs from a deterministic template.
ANTHROPIC_API_KEY = "sk-ant-..."
